name: Approval test flow
on:
  workflow_dispatch:

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo building
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: trstringer/manual-approval@v1
        with:
          secret: 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
          approvers: pmgitrep
      - name: Deploy to production
        run: echo deploying
